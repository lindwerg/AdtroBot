# Отчет о полном аудите Telegram бота AdtroBot

**Дата:** 24.01.2026
**Анализ:** Код + Live-тестирование через Telegram Web

---

## Выполненный анализ

### ✅ Изучен код:
- Все handlers (start, horoscope, tarot, natal, subscription, profile)
- Все keyboards (Reply и Inline)
- Все callbacks и FSM states
- Бизнес-логика (лимиты, кэширование, платежи)

### ✅ Live-тестирование:
- Onboarding flow (ввод даты рождения) - работает
- Первый гороскоп для Рыб - показался корректно
- Клавиатура выбора знаков (4×3) - **подтверждена UX проблема**

---

## Найденные проблемы

### 🔴 P0 - Критические (требуют немедленного исправления)

#### 1. Таймзоны: 8 кнопок вертикально (1×8)
**Файл:** `src/bot/keyboards/profile.py:12-30, 50-57`

**Проблема:**
```python
# Строка 56
builder.adjust(1)  # ВСЕ кнопки по одной вертикально!
```
- Бесконечный скролл на телефоне
- Текст кнопок очень длинный: "Екатеринбург (UTC+5)" - 24 символа
- Пользователь видит только 2-3 кнопки из 8 на экране

**Решение:**
- Изменить layout на 2×4 (2 кнопки в строке)
- Сократить текст: "Екатеринбург UTC+5" или "UTC+5 Екат."

**Код изменения:**
```python
# Было:
builder.adjust(1)

# Станет:
builder.adjust(2)  # по 2 кнопки в строку
```

---

#### 2. Неструктурированные callback_data (смешивание типов)
**Файлы:**
- `src/bot/handlers/start.py` - строка с `"get_first_forecast"`
- `src/bot/handlers/subscription.py` - строка с `"menu_subscription"`
- `src/bot/handlers/natal.py` - строка с `"setup_birth_data"`
- `src/bot/handlers/profile.py` - строки с raw strings

**Проблема:**
```python
# Смешивание подходов:
# ✅ Хорошо (структурированные):
callback_data=ZodiacCallback(s="Aries").pack()
callback_data=TarotCallback(a=TarotAction.CARD_OF_DAY).pack()

# ❌ Плохо (сырые строки):
callback_data="setup_birth_data"
callback_data="menu_subscription"
callback_data="get_first_forecast"
```

**Последствия:**
- Сложнее искать обработчики callbacks
- Нет type safety
- Нарушение паттерна

**Решение:**
Создать структурированные callbacks для всех меню:
```python
# src/bot/callbacks/__init__.py
class MenuAction(str, Enum):
    GET_FIRST_FORECAST = "first"
    MENU_SUBSCRIPTION = "sub"
    SETUP_BIRTH_DATA = "setup"
    PROFILE_NOTIFICATIONS = "notif"

class MenuCallback(CallbackData, prefix="menu"):
    action: MenuAction
```

---

### 🟡 P1 - Важные (значительные улучшения UX)

#### 3. Кнопки зодиака: только эмодзи без текста
**Файл:** `src/bot/keyboards/horoscope.py:43-50`

**Проблема (подтверждена live):**
```python
# Строка 47
text = f"{'✓ ' if name == current_sign else ''}{zodiac.emoji}"
# Результат: кнопки показывают только "♈", "♉", "♊" и т.д.
```
- Очень маленькие кнопки (только эмодзи)
- Сложно нажать на телефоне
- Галочка ✓ почти не видна на выбранном знаке
- Непонятно какой это знак без текста

**Визуальное доказательство:**
```
Текущая клавиатура (4×3):
┌────┬────┬────┬────┐
│ ♈  │ ♉  │ ♊  │ ♋  │
├────┼────┼────┼────┤
│ ♌  │ ♍  │ ♎  │ ♏  │
├────┼────┼────┼────┤
│ ♐  │ ♑  │ ♒  │✓♓ │  ← галочка почти не видна
└────┴────┴────┴────┘
```

**Решение (вариант 1 - рекомендуемый):**
```python
# Добавить название знака:
text = f"{zodiac.emoji} {zodiac.name_ru}"
if name == current_sign:
    text = f"✓ {text}"

# Результат: "✓ ♈ Овен", "♉ Телец" и т.д.
```

**Решение (вариант 2 - если не влезает):**
```python
# Изменить layout с 4×3 на 2×6
builder.adjust(2)  # вместо adjust(4)
```

---

#### 4. Натальная карта: 6 разных клавиатур для разных состояний
**Файл:** `src/bot/keyboards/natal.py`

**Проблема:**
```python
# 6 отдельных функций для разных состояний:
get_natal_menu_keyboard()         # когда не премиум
get_natal_setup_keyboard()        # когда нет данных
get_natal_teaser_keyboard()       # teaser для free
get_natal_with_buy_keyboard()     # premium без покупки
get_natal_with_open_keyboard()    # premium с покупкой
get_free_natal_keyboard()         # для бесплатных
```

**Последствия:**
- Много дублирования кода
- Сложно понять логику выбора клавиатуры
- Легко забыть обновить одну из 6 функций

**Решение:**
Создать единую фабрику:
```python
def get_natal_keyboard(
    user_status: str,        # "free" | "premium"
    has_birth_data: bool,
    has_detailed: bool,
    is_generating: bool = False
) -> InlineKeyboardMarkup:
    builder = InlineKeyboardBuilder()

    if user_status == "free":
        if not has_birth_data:
            builder.button(text="🔮 Настроить данные", ...)
        builder.button(text="✨ Оформить подписку", ...)
    elif user_status == "premium":
        if has_detailed:
            builder.button(text="📖 Открыть разбор", ...)
        else:
            builder.button(text="💎 Купить разбор", ...)

    builder.button(text="◀️ Назад", ...)
    return builder.as_markup()
```

---

#### 5. История таро: длинные строки с обрезкой
**Файл:** `src/bot/keyboards/tarot.py:92-95`

**Проблема:**
```python
# Строка 92-94
date_str = spread.created_at.strftime("%d.%m")
type_label = "CC" if spread.spread_type == "celtic_cross" else "3K"
question_preview = spread.question[:20] + "..." if len(spread.question) > 20 else spread.question
text = f"{date_str} [{type_label}] {question_preview}"

# Результат: "15.01 [CC] Когда встречу мужа..."
# Проблема: на узких экранах обрезается, нет индекса
```

**Последствия:**
- Предпросмотр на 20 символов часто неинформативен
- Плохо видно разницу между похожими вопросами
- Нет нумерации (неудобно ссылаться на расклад)

**Решение:**
```python
# Сократить предпросмотр, добавить индекс:
question_preview = spread.question[:15] + "..." if len(spread.question) > 15 else spread.question
text = f"[{idx+1}] {date_str} [{type_label}] {question_preview}"

# Результат: "[1] 15.01 [CC] Когда встречу..."
# Выгода: короче на 5 символов, есть нумерация
```

---

## Функциональные проверки из кода

### ✅ Работает корректно:

1. **Atomic лимиты таро** (race condition protection):
   ```python
   # src/services/payment/service.py
   UPDATE User SET tarot_spread_count = tarot_spread_count + 1
   WHERE User.id = ? AND tarot_spread_count < daily_limit
   ```

2. **Кэширование карты дня**:
   ```python
   # src/bot/handlers/tarot.py
   if user.card_of_day_date == today and user.card_of_day_id:
       # Используем кэш
   ```

3. **FSM states** для многошаговых сценариев:
   - Onboarding (ввод даты) - проверено live ✅
   - Birth data setup (время, город)
   - Tarot questions

4. **YooKassa webhook** с idempotency через payment_id

5. **Telegraph fallback** для длинных текстов (>4096)

---

## Что НЕ протестировано live (но код проверен):

### Требуют ручного тестирования после исправлений:
1. ⏸️ Модуль "Таро" (карта дня, 3-карты, кельтский крест, история)
2. ⏸️ Модуль "Натальная карта" (setup, генерация PNG, AI интерпретация)
3. ⏸️ Модуль "Подписка" (тарифы, создание платежа)
4. ⏸️ Модуль "Профиль" (настройки уведомлений, **таймзоны**)
5. ⏸️ Edge cases (невалидный ввод, AI timeout)

---

## План исправлений

### Итерация 1: P0 (30 минут)
1. ✅ Исправить таймзоны (2×4 сетка, короткий текст)
2. ✅ Создать MenuCallback и заменить raw strings

### Итерация 2: P1 (1 час)
3. ✅ Унифицировать клавиатуры натальной карты
4. ✅ Добавить текст к кнопкам зодиака
5. ✅ Улучшить форматирование истории таро

### Итерация 3: Верификация
6. ✅ Протестировать все исправления через Telegram Web
7. ✅ Проверить на мобильном (через dev tools)

---

## Оценка текущего состояния

### Архитектура: 7/10
- Хорошая структура модулей
- FSM states логичные
- Atomic операции для race conditions

### UX клавиатур: 5/10
- Таймзоны убивают UX (1×8 вертикально)
- Кнопки зодиака слишком маленькие
- История таро плохо читается

### Консистентность кода: 6/10
- Смешивание callback_data подходов
- 6 клавиатур для одного модуля (натальная карта)

### Функциональность: 8/10
- Все работает корректно
- Лимиты, кэширование, платежи - OK
- AI генерация с retry логикой

---

## Итог

**Критические проблемы (P0):** 2
**Важные улучшения (P1):** 3
**Функциональные баги:** 0 (найденных)

**Время на исправление:** ~1.5 часа
**Приоритет:** Начать с P0, затем P1

---

**Следующий шаг:** Немедленно приступить к исправлениям по плану.
